# PingPlotter Environment Configuration
# Copy this file to .env and update with your actual values

# Database Configuration (REQUIRED)
# PostgreSQL connection string
DATABASE_URL=postgresql://user:password@your-db-host:5432/pingplot

# Server Configuration
# Port for the web interface and API
PORT=9911

# Node.js environment
NODE_ENV=production

# Example configurations:
# Local PostgreSQL:
# DATABASE_URL=postgresql://postgres:password@localhost:5432/pingplot
#
# PostgreSQL in Docker network:
# DATABASE_URL=postgresql://postgres:password@postgres:5432/pingplot
#
# Remote PostgreSQL:
# DATABASE_URL=postgresql://user:password@192.168.1.100:5432/pingplot
#
# Using host.docker.internal (for databases on the same host):
# DATABASE_URL=postgresql://postgres:password@host.docker.internal:5432/pingplot
